<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <!-- Bundle identity (resolved at build time by Xcode) -->
    <key>CFBundleDisplayName</key>
    <string>StreamKitSample</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>

    <!-- Privacy usage strings -->
    <key>NSCameraUsageDescription</key>
    <string>Used to stream the camera feed.</string>
    <key>NSMicrophoneUsageDescription</key>
    <string>Used to stream microphone audio.</string>
    <key>NSEnterpriseMCAMUsageDescription</key>
    <string>Used to stream the main passthrough camera via ARKit.</string>

    <!-- App Transport Security
         Allows connections to local/LAN servers that use self-signed TLS certificates.
         This is intentional — StreamKit connects to developer-controlled servers on
         the local network and does not transmit data to the public internet. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
    </dict>

    <!-- SwiftUI scene support (equivalent to INFOPLIST_KEY_UIApplicationSceneManifest_Generation = YES) -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <true/>
        <key>UISceneConfigurations</key>
        <dict/>
    </dict>

    <!-- iOS interface orientations -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Misc -->
    <key>UISupportsIndirectInputEvents</key>
    <true/>
    <key>UIRequiresFullScreen</key>
    <false/>

</dict>
</plist>
