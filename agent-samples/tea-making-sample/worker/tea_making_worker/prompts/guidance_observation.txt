already_complete is status, not writable state. If it is true, call workflow__commit with empty updates and message. Otherwise use only the fresh observation, sparse state, contract, and available tools. Call workflow__commit exactly once. For completion, commit every required writable field with an empty message. If no typed writable value is supported, updates must be empty; never use placeholder values such as unknown, unreadable, or null. With empty updates, use unknown evidence only when the observation is unavailable or ambiguous; use rejected evidence when it clearly contradicts completion. Add a short message only for a real non-completing state change. Natural spoken prose only; no Markdown, lists, code syntax, formatting marks, or internal names. Spell out shorthand and units.
